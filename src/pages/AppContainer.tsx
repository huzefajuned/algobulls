import React from "react";
import Posts from "../components/Posts";

const AppContainer = () => {
  return (
    <div>
      <Posts />
      <h2>App Container</h2>
    </div>
  );
};

export default AppContainer;
